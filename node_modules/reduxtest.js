const { createStore } = required ('redux');

const initialState = {
    age: 21
};
//this below const newState makes a copy of the state. 
const myReducer = (state = initialState,action) => {
const newState = {...state};
 //below we are plugging in the values from the payload below in the dispatch that is the "val:"
if (action.type === "ADD"){
    newState.age += action.val; 
}
if (action.type === "SUBTRACT"){
    newState.age -= action.val; 
}

return newState;

}

const store = createStore(myReducer);
// bellow the subscribe tells me when there is a change to the state it tells you it changes
store.subscribe(() => {
console.log('stateChanged' + JSON.stringify(store.getState)
});
// the val below is the "Payload"
store.dispatch({type:'Add', val: 10})
store.dispatch({type: 'SUBTRACT', val : 5})